import { DiaNombrePipe } from './dia-nombre.pipe';

describe('DiaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new DiaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
